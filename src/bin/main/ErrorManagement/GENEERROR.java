package bin.main.ErrorManagement;

/**
 * Error types thrown while creating a gene.
 */
public enum GENEERROR {
    INVALID_CHARACTER, INVALID_ARM, INVALID_CHROMOSOME, INVALID_LOCATION, INVALID_FILE_FORMAT
}
